[package]
name = "yew-starter"
version = "0.1.0"
edition = "2024"

[dependencies]
yew = { version = "0.23.0", features = ["csr"] } # WebAssembly UI framework with client-side rendering.
yew-router = "0.20.0" # Client-side routing for Yew apps.
